{
  "name": "sme-copilot-malaysia",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev": "bash ./run-dev.sh",
    "dev:web": "npm run dev --prefix apps/web",
    "dev:chat": "npm run dev --prefix services/chat",
    "dev:telegram": "npm run dev --prefix services/telegram",
    "build": "npm run build --prefix apps/web && npm run build --prefix services/chat && npm run build --prefix services/telegram",
    "build:web": "npm run build --prefix apps/web",
    "build:chat": "npm run build --prefix services/chat",
    "build:telegram": "npm run build --prefix services/telegram"
  }
}
